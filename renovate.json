{
    "extends": ["config:base"],
    "enabledManagers": ["composer"],
    "packageRules": [
        {
            "matchDepTypes": [
                "require",
                "require-dev"
            ],
            "matchUpdateTypes": ["patch", "minor", "major"],
            "enabled": false
        }
    ],
    "vulnerabilityAlerts": {
        "enabled": true
    },
    "rangeStrategy": "bump"
}
